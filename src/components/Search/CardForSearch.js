import React from 'react';

const cardForSearch = (props) => {
    return(
        <div>
      <h1>{props.name}</h1>
            <p>some other detail</p><br/>
        </div>
    );
}

export default cardForSearch;